[package]
name = "react"
version = "0.1.0"
edition = "2021"
description = "React-like test harness for Rust-based integration tests"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "react"
path = "src/lib.rs"
