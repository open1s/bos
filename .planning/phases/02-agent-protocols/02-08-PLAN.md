---
phase: 02-agent-protocols
plan: "08"
type: execute
wave: 5
depends_on: []
files_modified:
  - .planning/phases/02-agent-protocols/02-01-PLAN.md
  - .planning/phases/02-agent-protocols/02-02-PLAN.md
  - .planning/phases/02-agent-protocols/02-03-PLAN.md
  - .planning/phases/02-agent-protocols/02-04-PLAN.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "All plans reference requirement IDs that exist in REQUIREMENTS.md"
    - "Plan 02-01 uses A2A-01, A2A-02, A2A-03, A2A-04"
    - "Plan 02-02 uses MCP-01, MCP-02, MCP-03"
    - "Plan 02-03 uses SKIL-01, SKIL-02, SKIL-03, SKIL-04"
    - "Plan 02-04 uses STRM-02, STRM-03"
  artifacts:
    - path: ".planning/phases/02-agent-protocols/02-01-PLAN.md"
      contains: "- A2A-01"
    - path: ".planning/phases/02-agent-protocols/02-02-PLAN.md"
      contains: "- MCP-01"
    - path: ".planning/phases/02-agent-protocols/02-03-PLAN.md"
      contains: "- SKIL-01"
    - path: ".planning/phases/02-agent-protocols/02-04-PLAN.md"
      contains: "- STRM-02"
  key_links:
    - from: ".planning/phases/02-agent-protocols/*-PLAN.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirements field valid"
---

<objective>
Fix requirement ID references in all Phase 02 plans to match actual requirements in REQUIREMENTS.md.

**Purpose:** Address verification gap where plans reference PROTO-01/02/03 but REQUIREMENTS.md only has A2A-*/MCP-*/SKIL-*/STRM-* requirements.

**Output:** All plans reference valid requirement IDs from REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/gaosg/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gaosg/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-agent-protocols/02-VERIFICATION.md
@.planning/REQUIREMENTS.md

# Gap Being Closed

From VERIFICATION.md (line 186):
"Plans reference PROTO-01/02/03 which don't exist in REQUIREMENTS.md - requirements are actually covered by the A2A/MCP/SKIL/STRM set."

# Map from Current Plans to Correct Requirements

From REQUIREMENTS.md traceability table:
- **A2A Protocol** (Phase 2):
  - A2A-01: A2A message envelope — JSON-RPC over Zenoh
  - A2A-02: Task state machine — Submitted → Working → Completed | Failed | InputRequired
  - A2A-03: Agent discovery — agents publish capabilities to agents/capabilities/{id}
  - A2A-04: Delegation — Agent A can delegate task to Agent B via bus

- **MCP Integration** (Phase 2):
  - MCP-01: MCP STDIO client — spawn MCP server process, send/receive JSON-RPC 2.0 messages
  - MCP-02: MCP tool adapter — convert MCP tool definitions to brainos Tool trait
  - MCP-03: MCP bridge — MCP tools registered transparently in ToolRegistry

- **Skills System** (Phase 2):
  - SKIL-01: Skill definition — loaded from TOML/YAML: name, description, prompt fragment
  - SKIL-02: Skill registry — load, validate, attach skills to agents
  - SKIL-03: Skill composer — merge skill prompts into agent system prompt
  - SKIL-04: Skill namespacing — avoid conflicts by prefixing tools with skill name

- **Streaming** (Phase 2):
  - STRM-02: Token streaming over bus — agent can stream tokens to subscribers via PublisherWrapper
  - STRM-03: Backpressure handling — don't flood bus with per-token messages

# Current Incorrect References

From grep of plan files:
- 02-01-PLAN.md line 18: `requirements: PROTO-01`
- 02-02-PLAN.md line 17: `requirements: PROTO-02`
- 02-03-PLAN.md line 18: `requirements: PROTO-03`
- 02-04-PLAN.md: Correctly uses `requirements: STRM-02, STRM-03`

# Correct Mapping

- 02-01-PLAN.md (A2A Protocol): Should use A2A-01, A2A-02, A2A-03, A2A-04
- 02-02-PLAN.md (MCP Bridge): Should use MCP-01, MCP-02, MCP-03
- 02-03-PLAN.md (Skills System): Should use SKIL-01, SKIL-02, SKIL-03, SKIL-04
- 02-04-PLAN.md (Streaming): Already correct with STRM-02, STRM-03

<tasks>

<task type="auto">
  <name>Task 1: Fix requirements in Plan 02-01 (A2A Protocol)</name>
  <files>.planning/phases/02-agent-protocols/02-01-PLAN.md</files>
  <read_first>
    - .planning/phases/02-agent-protocols/02-01-PLAN.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Update the requirements frontmatter field in 02-01-PLAN.md:

**Current (line 18):**
```yaml
requirements:
  - PROTO-01
```

**Change to:**
```yaml
requirements:
  - A2A-01
  - A2A-02
  - A2A-03
  - A2A-04
```

**Verification:**
- A2A-01 maps to A2A message envelope implementation
- A2A-02 maps to Task state machine
- A2A-03 maps to Agent discovery
- A2A-04 maps to Delegation functionality

All of these are implemented in the tasks of Plan 02-01.
  </action>
  <verify>
    <automated>
      grep -A4 "^requirements:" .planning/phases/02-agent-protocols/02-01-PLAN.md | grep "A2A-01"
      grep -A4 "^requirements:" .planning/phases/02-agent-protocols/02-01-PLAN.md | grep "A2A-04"
      ! grep "PROTO-01" .planning/phases/02-agent-protocols/02-01-PLAN.md
    </automated>
  </verify>
  <done>
    - Requirements field now lists A2A-01, A2A-02, A2A-03, A2A-04
    - No references to PROTO-01 remain
    - All requirements exist in REQUIREMENTS.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix requirements in Plan 02-02 (MCP Bridge)</name>
  <files>.planning/phases/02-agent-protocols/02-02-PLAN.md</files>
  <read_first>
    - .planning/phases/02-agent-protocols/02-02-PLAN.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Update the requirements frontmatter field in 02-02-PLAN.md:

**Current (line 17):**
```yaml
requirements:
  - PROTO-02
```

**Change to:**
```yaml
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
```

**Verification:**
- MCP-01 maps to MCP STDIO client implementation
- MCP-02 maps to MCP tool adapter
- MCP-03 maps to MCP bridge (ToolRegistry integration)

All of these are implemented in the tasks of Plan 02-02.
  </action>
  <verify>
    <automated>
      grep -A4 "^requirements:" .planning/phases/02-agent-protocols/02-02-PLAN.md | grep "MCP-01"
      grep -A4 "^requirements:" .planning/phases/02-agent-protocols/02-02-PLAN.md | grep "MCP-03"
      ! grep "PROTO-02" .planning/phases/02-agent-protocols/02-02-PLAN.md
    </automated>
  </verify>
  <done>
    - Requirements field now lists MCP-01, MCP-02, MCP-03
    - No references to PROTO-02 remain
    - All requirements exist in REQUIREMENTS.md
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix requirements in Plan 02-03 (Skills System)</name>
  <files>.planning/phases/02-agent-protocols/02-03-PLAN.md</files>
  <read_first>
    - .planning/phases/02-agent-protocols/02-03-PLAN.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Update the requirements frontmatter field in 02-03-PLAN.md:

**Current (line 18):**
```yaml
requirements:
  - PROTO-03
```

**Change to:**
```yaml
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04
```

**Verification:**
- SKIL-01 maps to Skill definition (SkillMetadata, SKILL.md format)
- SKIL-02 maps to Skill registry (SkillLoader, discovery)
- SKIL-03 maps to Skill composer (SkillInjector, system prompt injection)
- SKIL-04 maps to Skill namespacing (avoid tool name conflicts)

All of these are implemented in the tasks of Plan 02-03.
  </action>
  <verify>
    <automated>
      grep -A4 "^requirements:" .planning/phases/02-agent-protocols/02-03-PLAN.md | grep "SKIL-01"
      grep -A4 "^requirements:" .planning/phases/02-agent-protocols/02-03-PLAN.md | grep "SKIL-04"
      ! grep "PROTO-03" .planning/phases/02-agent-protocols/02-03-PLAN.md
    </automated>
  </verify>
  <done>
    - Requirements field now lists SKIL-01, SKIL-02, SKIL-03, SKIL-04
    - No references to PROTO-03 remain
    - All requirements exist in REQUIREMENTS.md
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify Plan 02-04 requirements are correct</name>
  <files>.planning/phases/02-agent-protocols/02-04-PLAN.md</files>
  <read_first>
    - .planning/phases/02-agent-protocols/02-04-PLAN.md
  </read_first>
  <action>
Verify that Plan 02-04 already has correct requirements and document it:

**Expected (already correct from earlier verification):**
```yaml
requirements:
  - STRM-02
  - STRM-03
```

**Verification:**
- STRM-02 maps to Token streaming over bus (PublisherWrapper)
- STRM-03 maps to Backpressure handling (BackpressureController)

Both are implemented in the tasks of Plan 02-04.

**Action:** Just verify, no changes needed for this plan.
  </action>
  <verify>
    <automated>
      grep -A3 "^requirements:" .planning/phases/02-agent-protocols/02-04-PLAN.md | grep "STRM-02"
      grep -A3 "^requirements:" .planning/phases/02-agent-protocols/02-04-PLAN.md | grep "STRM-03"
    </automated>
  </verify>
  <done>
    - Verified: Plan 02-04 already has correct requirements (STRM-02, STRM-03)
    - No changes needed
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify all plans use valid requirement IDs</name>
  <files>.planning/phases/02-agent-protocols/*.md</files>
  <read_first>
    - All Phase 02 plan files
    - REQUIREMENTS.md
  </read_first>
  <action>
Perform comprehensive verification to ensure no PROTO-* references remain and all requirement IDs are valid:

**Verification script:**
```bash
# Check for any PROTO-* references (should return nothing)
rg "PROTO-\d+" .planning/phases/02-agent-protocols/

# Extract all requirement IDs from plans
rg "^requirements:" .planning/phases/02-agent-protocols/ -A 3

# Verify all IDs exist in REQUIREMENTS.md
grep -E "^\- \*\*(A2A|MCP|SKIL|STRM)-\d+\*\*:" .planning/REQUIREMENTS.md
```

**Expected output:**
- No PROTO-* references found
- All plans have requirements: field listing valid IDs
- IDs match those in REQUIREMENTS.md traceability table
  </action>
  <verify>
    <automated>
      ! rg "PROTO-\d+" .planning/phases/02-agent-protocols/
      rg "A2A-\d+|MCP-\d+|SKIL-\d+|STRM-\d+" .planning/phases/02-agent-protocols/ | grep "requirements:" -A 2
    </automated>
  </verify>
  <done>
    - All PROTO-* references removed from all plans
    - All requirement IDs are valid (exist in REQUIREMENTS.md)
    - Each plan maps to correct set of requirements by subsystem
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **No PROTO references**:
   ```bash
   rg "PROTO-\d+" .planning/phases/02-agent-protocols/
   # Should return no results
   ```

2. **All plans have valid requirements**:
   ```bash
   for plan in .planning/phases/02-agent-protocols/*-PLAN.md; do
     echo "=== $plan ==="
     rg "^requirements:" "$plan" -A 3
   done
   # Should show valid requirement IDs for each plan
   ```

3. **Verify mapping**:
   - 02-01-PLAN.md: A2A-01, A2A-02, A2A-03, A2A-04 ✓
   - 02-02-PLAN.md: MCP-01, MCP-02, MCP-03 ✓
   - 02-03-PLAN.md: SKIL-01, SKIL-02, SKIL-03, SKIL-04 ✓
   - 02-04-PLAN.md: STRM-02, STRM-03 ✓
   - 02-05-PLAN.md: A2A-01, A2A-02, A2A-03 (gap closure) ✓
   - 02-06-PLAN.md: SKIL-01, SKIL-02, SKIL-03, SKIL-04 (gap closure) ✓
   - 02-07-PLAN.md: A2A-*, STRM-* (gap closure) ✓
   - 02-08-PLAN.md: (no requirements, documentation fix) ✓
   ```

4. **Verify against REQUIREMENTS.md**:
   ```bash
   # All requirement IDs should match these exact formats
   rg "^\- \*\*(A2A|MCP|SKIL|STRM)-\d+\*\*:" .planning/REQUIREMENTS.md
   ```
</verification>

<success_criteria>
Requirements mapping gap closed:
- All 8 plans (4 original + 4 gap closure) have valid requirement IDs
- No PROTO-* references remain anywhere in Phase 02
- Each plan's requirements match the subsystem it implements
- All requirement IDs exist in REQUIREMENTS.md traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-protocols/02-08-SUMMARY.md` noting:
- 3 plans updated (02-01, 02-02, 02-03)
- 1 plan verified correct (02-04)
- All references to PROTO-* removed
- All requirements now match REQUIREMENTS.md
</output>
